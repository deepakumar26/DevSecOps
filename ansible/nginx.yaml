- name: To install Nginx

  hosts: 172.31.27.150
  become: true


  tasks:
    - name: Install Nginx package
      ansible.builtin.dnf:
        name: nginx
        state: latest

  # tasks:
  #   - name: Install Nginx package
  #     apt:
  #       name: nginx
  #       state: present
  #     notify:
  #       - Start Nginx

  # handlers:
  #   - name: Start Nginx
  #     service:
  #       name: nginx
  #       state: started